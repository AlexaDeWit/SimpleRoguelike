using System;

namespace GameRenderer {
	/*
	 * Consider using a dictionary to associate tile objects with their display content,
	 * and generally doing this for all content. it will let me abstract it from its base data
	 * holy shit I'm a genius, no more divergent classtypes
	 */
	public class GraphicalRenderer : Renderer {
		public GraphicalRenderer () {
		}
	}
}

